#!/usr/bin/env bash

echo "detecting at ... "
echo $1

ls -l $1

if [ ! -f $1/*.lid ]; then
  echo "I need a *.lid"
  exit 1
fi

#if [ ! -f $1/Procfile ]; then
#  echo "I need a Procfile"
#  exit 1
#fi

exit 0
